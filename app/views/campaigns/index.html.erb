<div class="row">
	<div class="small-12">
		<h3><%= link_to "New Campaign", new_campaign_path(@campaign) %></h3>
	</div>
</div>

<% @campaigns.each do |c| %>
<div class="row">
	<div class="small-12 columns">
		<h3><%= c.title %></h3>
		<h4><%= link_to 'View', campaign_path(c.id) %></h4>
	</div>
</div>

<% end %>


